export interface SuccessStory {
  id: string;
  text: string;
}

/** No real success-story backend exists yet — kept empty rather than showing fabricated stories. */
export const SEED_SUCCESS_STORIES: SuccessStory[] = [];
