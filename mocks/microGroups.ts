import type { MicroGroupSuggestion } from '../types';

/** Local-only fallback seed shown before the first real backend fetch — no fabricated groups. */
export const SEED_MICRO_GROUPS: MicroGroupSuggestion[] = [];
