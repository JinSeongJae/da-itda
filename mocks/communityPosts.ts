import type { CommunityPost } from '../types';

/** Local-only fallback seed shown before the first real backend fetch — no fabricated posts. */
export const SEED_COMMUNITY_POSTS: CommunityPost[] = [];
